# Google Analytics 4 Measurement ID (looks like G-XXXXXXXXXX).
# Copy this file to `.env` and fill in your ID. Analytics only loads in
# production builds (npm run build), never during local dev.
VITE_GA_ID=
